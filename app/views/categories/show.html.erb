<h3>Jobs under this category</h3>

<% Job.where(category_id: @category.id).each do |job| %>
  <li><%= job.title %></li>  
<% end %>

<p id="notice"><%= notice %></p>

	<p>
	  <strong>Name:</strong>
  	<%= @category.name %>
	</p>
  <p>
    <strong>Desc:</strong>
	  <%= @category.desc %>
	</p>

	<%= link_to 'Edit', edit_category_path(@category) %>
 |
	<%= link_to 'Back', categories_path %>
